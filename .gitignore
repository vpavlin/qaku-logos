# nix build outputs
result
result-*
qaku_core/result*
module/result*

# node / expo
node_modules/
mobile/node_modules/
.expo/

# expo prebuild output (regenerated from app.json + config plugins)
mobile/android/
mobile/ios/

# build artifacts / release staging
dist/
*.apk

# native delivery .so live in mobile/native/logosdelivery/arm64-v8a (tracked)
