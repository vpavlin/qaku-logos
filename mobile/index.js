import { registerRootComponent } from "expo";
import App from "./App";

// QAKU mobile entry point. registerRootComponent calls AppRegistry.registerComponent
// and sets up the Expo environment for both Expo Go and a native (prebuild) build.
registerRootComponent(App);
